# Acceptance — scored 2026-08-22

Method: ten criteria, 0–10 each, total /100. Scored from the live Hostkey site, the ident packs, and secondlook LOOK-0006 (maker onramp). Not CONFIRMED; evidence grade LOW until a cold maker runs capture from a clean checkout.

| # | Criterion | /10 | Notes |
|---|---|---|---|
| 1 | Scene file is the store; the mp4 is a view, not a prompt | 8 | Hero and Make name `scene.json`. |
| 2 | A named tweak is a recapture, not another generate | 8 | Type overlay off, same camera. Pair plus the field. |
| 3 | The same image can be recreated from the scene | 8 | Mark stills: three orbs, type on then off. |
| 4 | A later renderer can enhance without new inference | 5 | Claimed. One recapture shown. Not two renderer generations. |
| 5 | Public story is reuse and steering, not Imagine cheapness | 8 | “Keep the shot.” No 160,000×. |
| 6 | Film first, then a recapture receipt | 8 | Live order: v6 → proof → make → scenes → store. |
| 7 | A cold viewer can say what Eidos Video is after one scroll | 7 | Clip is a render of a scene file. |
| 8 | A cold maker can name write → capture → tweak → recapture | 8 | Make chapter. Execution still needs a local clone. |
| 9 | The real capture command is on the public face | 9 | Exact `video-3d-forge` README: `node bin/capture.mjs` + assemble. |
| 10 | Live ident plays; pictured ident has no burned-in type | 9 | HTTPS 200. Before-still may show type. |

**Total: 78 / 100**

Pass bar for the north star: **80**, with criterion 8 ≥ 7 and criterion 9 ≥ 7. Maker path clears 8 and 9. Total is still under 80 until a second renderer generation is shown or a cold maker actually runs capture.

False greens: HTML 200 with a broken mp4; a recapture pair whose scene files differ in camera, not type; a how-to that invents a capture command.
