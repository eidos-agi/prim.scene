# Log

- 2026-08-21 — first scene: flying ident, 16s, sage graze. Dual-authority debt: renderer.html still hardcodes keys that must match scene.json.
- 2026-08-21 — v2: renderer reads scene.json. Closer open, banked fly, type from spec. Dual-authority debt closed for camera/type/duration.
- 2026-08-21 — LOOK-0001 P1: camera roll on keys; Catmull-Rom (no stop at knots); dust confined to lamp volume; copy bound to spec.type.
- 2026-08-22 — ident-v3: object keys in scene.json. Left brass grows out of mid (not sage, not rest-slot spawn). Brass lockup Y is pre-compensated for group.rotation_z_deg -6 so getWorldPosition Y matches (LOOK P1). Sage static Y raised so praxis stays the odd height in world space. Renderer micro-bob (±0.028) only if sage has no keys. Bed unchanged (sha 03d1c405bfaa5a60945e79bcf0d8043c10fd06fb).
- 2026-08-22 — ident-v4: 1 then 2 then 3. Fast yaw held ~3.4s, then omega PID damps, angle lock when slow. Two brass even Y; sage keyed in late, slightly higher (boss). Group keys baked by proof/spin_pid.py. Camera watches from the front. Bed unchanged.
- 2026-08-22 — ident-v4 crescendo lock: bed RMS peaks at t=12.0s (same as the name). Hold omega 16.2 through the groove until t=6.9; PID slows through the 7–12s swell; sage full at the hit. Rest is the emotion, not an arbitrary clock.
- 2026-08-22 — ident-v4 meander: the SCENE spins, not the balls. Camera azimuth PID + wander on radius/height/roll. Group yaw keys removed. Lockup still the mark, two even, sage raised.
- 2026-08-22 — ident-v4 accretion: 3600 seeded Kepler points. Groove streams the disk, swell tightens, 12s peak coalesces to one; that one peels into two even brass then sage boss. Camera high/wide on the disk, close on the one, pull back to the three.
- 2026-08-22 — ident-v4 ship + morph + logo PID: camera flies through the disk like a cockpit. Circles (instanced CircleGeometry) morph onto a Fibonacci sphere then the one. Group yaw PID-spins the completed mark down to rest at 16s.
- 2026-08-22 — ident-v4 cockpit pass: larger solid circles (dot_r=0.18), camera already in the volume (FOV 70, banked, floor off until lockup). Formal morph is spherical-coord wrap (disk longitude → Fibonacci latitude, inner first) onto the one — not a cartesian collapse. Logo yaw rips at 12.5–13.8s then Hermite rest-to-2π (omega=0, facing) at 16s.
- 2026-08-22 — ident-v4 vehicle cockpit: camera keys baked from (p,v,q) — lagged accel, velocity curves toward forward, bank from yaw/strafe, mild Kepler plasma + softened gravity. Not `pos += dir*speed`. Circles streak along v_flow − v_cam; approaching side Doppler-brighter. Canopy HUD fades as lockup. Logo PID unchanged.
- 2026-08-22 — ident-v4 highway: jarring edge-on disk replaced with a 3-lane brass/sage ribbon (Rainbow Road camera: look-ahead + curvature bank; Guitar Hero notes in lanes, beat pulse). Ribbon coils into accretion; notes wrap onto one at 12s. Logo PID unchanged.
- 2026-08-22 — ident-v4 log-spiral (Fable): highway-then-coil replaced with one equiangular spiral, one revolution, SPEED 9.0 then bleed after t=4. Look-ahead scales with radius. Daniel: more speed at the open, spin naturally into the center.
- 2026-08-22 — versioning: freeze that cut as `versions/ident-v5-log-spiral/` + `renders/ident-v5.mp4` + Desktop `eidos-agi-ident-v5.mp4`. Never overwrite.
- 2026-08-22 — ident-v6 skater (Fable): contracting Fibonacci shell Rs 2.8→1.02, ω ∝ 1/R² from t=8.2 peaks 3.4 rad/s at t=12, Hermite rest to −4π at 16s. New filename ident-v6.mp4.
